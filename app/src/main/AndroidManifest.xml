<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WasteManagementSystem"
        tools:targetApi="31"
        android:networkSecurityConfig="@xml/network_security_config">>
    <activity android:name=".activity.Main" android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>
    </activity>

        <activity android:name=".activity.UserLogin"></activity>
        <activity android:name=".activity.UserSignup"></activity>
        <activity android:name=".activity.UserResetPassword"></activity>
        <activity android:name=".activity.AdminResetPassword"></activity>
        <activity android:name=".activity.AdminCategory"></activity>
        <activity android:name=".activity.AdminDashboard"></activity>
        <activity android:name=".activity.UserDashboard"></activity>
        <activity android:name=".activity.AdminSignup"></activity>
        <activity android:name=".activity.AdminLogin"></activity>

    </application>

</manifest>